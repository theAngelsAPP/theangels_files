<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Default color will be tinted in code -->
    <solid android:color="@color/event_default_color" />
    <corners android:radius="10dp" />
</shape>
